React Native + Three.js(Fiber) 환경 세팅

안드로이드 에뮬레이터에서 3D 렌더링 시 초기화 문제 → 디버깅 방법(logcat 활용).

디버깅용 큐브 먼저 띄워서 화면 정상 출력 여부 확인 후 타일/카메라 조정.

아이소메트릭 뷰

전통적인 ISO 각도: X = 35.264°, Y = 45°.

프러스텀과 카메라 위치를 조정해 가로/세로 비율 원하는 대로 맞추는 방법 고민.

줌 & 패닝

확대/축소 범위 제한, 패닝 제약 풀기.

추가 구현 아이디어

랜덤 마커(큐브) 배치, 클릭 시 하이라이트 및 모달창 노출.

조건별 모달: 마커가 있으면 “물주기”, 없으면 “나무심기”.