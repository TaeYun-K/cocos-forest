오늘 한 일
- 프로젝트 빌드 및 실행 환경 점검
- 카드 내역 조회 기능 구현
- 카드와 계좌 연결 기능 연동

배운 점
- 빌드 과정에서 환경 의존성 확인의 중요성을 다시 인식
- 카드 내역 조회 시 API 응답 구조와 실제 화면 표시 데이터가 다를 수 있어 변환 과정 필요
- 카드와 계좌 연결 과정에서 사용자 선택 → API 호출 → 응답 처리 순서가 명확히 설계되어야 함

이슈
- 카드/계좌 연결 시 잘못된 입력 값 전달로 에러 발생 → 입력값 검증 로직 강화 필요

느낀 점
- 단순히 API 연동이 아니라 전체 흐름(빌드 → 실행 → 조회 → 연결)을 점검하면서 작업해야 안정적으로 기능이 완성됨
- 사용자 입장에서 카드·계좌 연동은 핵심 기능이므로 예외 처리와 UX 개선이 더욱 중요함